import {
  PDFGeneratorFontFamily,
  PDFGeneratorLogoPosition,
  PDFGeneratorOrientation,
  PDFGeneratorPageSize,
} from '../../constant'

export const pageSizeOptions = Object.values(PDFGeneratorPageSize)
export const orientationOptions = Object.values(PDFGeneratorOrientation)
export const fontFamilyOptions = Object.values(PDFGeneratorFontFamily)
export const logoPositionOptions = Object.values(PDFGeneratorLogoPosition)
